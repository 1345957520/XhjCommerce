@model IList<CategoryModel>

@using Chj.Web.Models.Catalog;
@if (Model.Count > 0)
{
    var cat_i = 0;
    foreach (var itemOne in Model)
    {
        <div class="row" style="margin-bottom:20px;">
          <div class="col-md-2" style="background-color:yellow">
              <div style="background-color:blue">
                  <span><a href="@Url.RouteUrl("Category", new { SeName = itemOne.SeName })">@itemOne.Name</a></span>
              </div>
              <ul>
                  @{
                    var cc = 0;
                  }
                  @foreach (var item in itemOne.SubCategories)
                  {
                      cc++;
                      @*if (cc % 2 != 0)
                      {
                          
                      }*@
                      <li>
                          <span><a href="@Url.RouteUrl("Category", new { SeName = item.SeName })">@item.Name</a></span>
                      </li>
                  }
              </ul>
          </div>
            <div class="col-md-10" style="background-color:red">.col-md-10</div>
        </div>
    }
    /*
    var cat_i = 0;
    foreach (var itemOne in Model)
    {
        <div class="row">
            <!--recommended_items-->

            <div class=".col-lg-6 .col-md-6" style="background-color:yellow;">
                <div>
                    <span><a href="@Url.RouteUrl("Category", new { SeName = itemOne.SeName })">@itemOne.Name</a></span>
                </div>
                <div class="">
                    @foreach (var item in itemOne.SubCategories)
                    {
                        <div class=".col-md-6">
                            <span><a href="@Url.RouteUrl("Category", new { SeName = item.SeName })">@item.Name</a></span>
                        </div>
                    }
                </div>
            </div>

            <div class=".col-lg-6 .col-md-6" style="background-color:red;">
                <a>11231</a>
            </div>

            
        </div>
    }
     */





}






